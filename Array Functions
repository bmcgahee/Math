#Author: Ben McGahee
#Title: Array Functions
#Date: 3/17/2016
#Purpose: These are some theoretical functions you can use for arrays such as swapping elements, shifting elements left and right, 
#converting other elements to first element, and comparing a given array to a random array to see if they are equivalent.  

#Converts 2nd, 3rd, 4th up to nth elements as the same as the first element.
def same(array):
    size = len(array)
    print "Before Loop: All elements are different"
    print array

    for i in range(0, size - 1):
        print "Loop " + str(i + 1) + ": " + str(array[i + 1]) + " becomes a " + str(temp)
        array[i] = array[i + 1]
        print array

#Moves the first element to the last index.
def moveFirstToLast(array):
    length = len(array)
    for i in range(0, length - 1):
            temp = array[i + 1]
            array[i + 1] = array[i]
            array[i] = temp
    return array

#Swaps the first and last elements.
def swapFirstAndLast(array):
    size = len(array)
    temp1 = array[0]
    temp2 = array[size - 1]
    array[size - 1] = temp1
    array[0] = temp2
    return array

#Shifts all elements 1 unit to the left. 
def leftShift(array):
    from collections import deque
    dq = deque(array)
    dq.rotate(-1)
    left = list(dq)
    print left

#Shifts all elements 1 unit to the right.
def rightShift(array):
    from collections import deque
    dq = deque(array)
    dq.rotate(1)
    right = list(dq)
    print right

#Compares a given array and a random array generated by the original array's elements 
#to see if they are equivalent or if and how they are different.
def compare(array):
    import random
    length = len(array)
    randomArray = []
    diffElements = []

    for n in range(0, length):
        randomValue = random.choice(array)
        randomArray.append(randomValue)

    if array == randomArray:
        print "The arrays are identical."
    else:
        for i in range(0, length):
            if array[i] != randomArray[i]:
                diffElements.append(randomArray[i])
        size = len(diffElements)
        print "Here is the random array:"
        print randomArray
        print "There are " + str(size) + " elements in the random array that are different."
        print "Here are the elements:"
        print diffElements
